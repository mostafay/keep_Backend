{
  "name": "keep",
  "version": "1.0.0",
  "description": "Flutter app with Node.js backend",
  "scripts": {
    "start": "node server.js",
    "build": "npx puppeteer browsers install chrome"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
